# Everything the image build does not need. Keeping the context small also
# keeps a stale host `target/` (gigabytes, and built against the wrong libc)
# out of the builder.
target
.git
.github
docs
AGENTS.md
.gitignore
.dockerignore
Dockerfile
